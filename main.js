const fill = document.querySelector('.fill');
const empty = docoment.querySelectorAll('.empty');

fill.addEventListener('dragstart', dragStart());
fill.addEventListener('dragend', dragEndtart());

function dragStart() {
   
}

function dragEnd() {

}